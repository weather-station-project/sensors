[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
colorlog = "~=6.0"
requests = "~=2.0"
rpi-bme280 = "~=0.0"
"w1thermsensor[async]" = "~=2.0"
gpiozero = "~=2.0"
aiohttp = "~=3.0"
tenacity = "~=9.0"
# Conditionally install RPi.GPIO only on Raspberry Pi
RPi.GPIO = { version = "~=0.0", markers = "platform_machine ~= 'arm' and sys_platform === 'linux'" }

[dev-packages]
black = "~=24.0"
flake8 = "~=7.0"
pre-commit = "~=4.0"

[requires]
python_version = "3.12"
